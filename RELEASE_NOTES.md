# 패미컴 탐정 클럽: 뒤에 선 소녀 한국어 패치 v0.9.0

Nintendo Switch판 「패미컴 탐정 클럽: 뒤에 선 소녀」를 위한
비공식 한국어 패치입니다.

이번 첨부파일은 지금까지 확인된 수정 사항을 합친 통합본입니다.

- 타이틀·장소명 애니메이션과 추가 조사·화면 그래픽 한글화
- 일부 대사 오역, 줄바꿈과 표시 간격 수정

## 지원 대상

- 게임 버전: Application v0 / 게임 표시 1.0.0
- 타이틀 ID: `010078400F7B0000`
- 패치 형식: Atmosphère LayeredFS

## Nintendo Switch·Atmosphère 적용

1. 아래 첨부파일 `FTC02_Korean_LayeredFS.zip`을 내려받아 압축을 풉니다.
2. 압축을 풀면 나오는 `atmosphere` 폴더를 SD 카드 최상위에 복사합니다.
3. 기존 패치 사용자는 같은 이름의 파일을 덮어씁니다. 저장 데이터는
   패치 폴더와 별도이므로 이 작업으로 삭제되지 않습니다.
4. SD 카드의 최종 경로가 다음과 같은지 확인합니다.

   ```text
   /atmosphere/contents/010078400f7b0000/romfs/
   ```

5. 게임이 실행 중이었다면 홈 메뉴에서 완전히 종료한 뒤 다시 실행합니다.
   본체를 재부팅할 필요는 없지만, 실행 중인 게임에는 새 파일이 즉시
   반영되지 않습니다.

## Eden·Yuzu 계열 에뮬레이터 적용

1. ZIP 안에서 다음 위치의 `romfs` 폴더를 찾습니다.

   ```text
   atmosphere/contents/010078400f7b0000/romfs
   ```

2. 에뮬레이터에서 대상 게임의 `Open Mods Directory`, `Open Mod Data
   Location` 등 게임별 모드 폴더 열기 기능을 사용합니다.
3. 타이틀 ID `010078400F7B0000` 아래에 `FTC02 Korean Patch` 같은 모드
   폴더를 만들고, 그 안에 ZIP의 `romfs` 폴더를 복사합니다.

   ```text
   <에뮬레이터 사용자 폴더>/load/010078400F7B0000/
   └─ FTC02 Korean Patch/
      └─ romfs/
         └─ ...
   ```

4. 기존 패치를 갱신할 때는 기존 `romfs`에 새 `romfs`의 내용을
   덮어씁니다.
5. 게임별 추가 콘텐츠 또는 모드 설정에서 해당 패치가 활성화됐는지
   확인하고, 게임과 에뮬레이터를 완전히 종료했다가 다시 실행합니다.

패치가 적용되지 않으면 타이틀 ID가 `010078400F7B0000`인지, 게임 표시
버전이 `1.0.0`인지, `romfs/romfs`처럼 폴더가 이중으로 들어가지 않았는지
확인하십시오. 모드 폴더 안에는 `atmosphere`가 아니라 `romfs`가 바로
있어야 합니다.

## 제거

- Switch: `/atmosphere/contents/010078400f7b0000/` 폴더를 제거하거나
  다른 이름으로 옮깁니다.
- 에뮬레이터: `FTC02 Korean Patch` 모드를 비활성화하거나 해당 모드
  폴더를 제거합니다.

전체 실기 플레이 검수는 계속 진행 중이므로 일부 오역이나 표시 문제가
추가로 발견될 수 있습니다.

이 배포물에는 게임 본체, NSP, XCI, NCA, 키, 펌웨어와 저장 데이터가
포함되어 있지 않습니다.
